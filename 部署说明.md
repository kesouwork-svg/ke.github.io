# ROI 计算器 - EdgeOne 部署说明

前端已就绪(`index.html` / `login.html` / `css/` / `images/`),后端为边缘函数 `worker.js` + KV 存储。本说明面向部署人员,按步骤操作即可。

## 一、准备

| 项 | 说明 |
|---|---|
| 腾讯云账号 | 需开通 EdgeOne(官网控制台) |
| 域名 | 建议用公司已有备案域名;没有可先用 EdgeOne 提供的默认测试域名 |
| 项目文件 | 整个 `Web_Project_ROI` 目录(含 index.html / login.html / css / images / worker.js) |

## 二、开通与创建 KV

1. EdgeOne 控制台 → 创建站点(绑定域名),完成域名接入(按要求做 DNS 解析)
2. 左侧「数据存储 → KV 存储」→ 新建 KV 命名空间,如 `roi-kv`(记录下命名空间 ID)
3. KV 无需建表,键会自动写入。初始由边缘函数首次调用时自动创建管理员

## 三、部署静态页面(EdgeOne Pages)

1. 控制台 → EdgeOne Pages → 新建项目
2. 上传 `Web_Project_ROI` 目录中**除 worker.js 外**的静态文件:
   - `index.html`、`login.html`、`css/style.css`、`images/logo.png`
3. 部署后得到一个 Pages 地址(或绑定自有域名),访问应能看到登录页

## 四、部署边缘函数

1. 控制台 → 边缘函数 → 新建函数
2. 代码框粘贴 `worker.js` 全部内容,保存
3. 配置**变量与绑定**:
   - 绑定 KV:命名空间选 `roi-kv`,变量名填 `KV`(代码中读取 `env.KV`)
   - 环境变量:`INVITE_CODE = ROI2026`(可改成公司内部码)
4. 发布函数

## 五、配置路由

在站点/域名的「规则引擎」或函数发布页配置:

| 路径 | 行为 |
|---|---|
| `/api/*` | 交给边缘函数处理 |
| 其余全部 | 走 Pages 静态(托管页面) |

> 这样 `index.html` 等静态资源由 Pages 提供,所有 `/api/...` 请求由边缘函数处理,两者同域,前端无需跨域配置。

## 六、初始化与账号

1. 首次访问任意 `/api` 接口时,自动创建管理员:`admin / admin123`(首次登录后请**立即改密码**——用管理员账号登录后在「账号管理」中可给任意账号重置密码)
2. 邀请码默认为 `ROI2026`(可在函数变量中改)
3. 把邀请码发给设备组同事 → 他们在登录页「注册申请」填信息提交 → 管理员在主页「账号管理」审核批准并定级(1级/2级)

## 七、数据迁移(把现有本地数据搬上云)

本地演示版的数据在浏览器 localStorage(`roi_calculator_projects`)。迁移方式:

1. 在有数据的浏览器打开本地版页面,控制台执行:
   ```js
   console.log(localStorage.getItem('roi_calculator_projects'))
   ```
2. 复制输出到部署后的服务器侧,一次性导入 KV(或在管理员后台提供导入功能,按需开发)

## 八、上线检查清单

- [ ] 未登录访问 `index.html` → 自动跳 `login.html`
- [ ] `admin/admin123` 能登录;注册新号走"待审核"
- [ ] 管理员审核:批准(1/2级)/驳回/改级/停用/重置密码 均可用
- [ ] 新建项目填负责人;列表标题右侧显示负责人槽型标签
- [ ] 1级可改所有项目;2级只能改自己负责的项目(其余只读)
- [ ] 数据保存后刷新仍在(KV 持久化)
- [ ] 建议开启 HTTPS(EdgeOne 默认支持)

## 九、日常运维

- **数据备份**:KV 控制台支持导出,建议每周导出一份 `projects`
- **密码安全**:正式使用前务必修改 admin 初始密码;必要时用「账号管理→重置密码」定期轮换
- **邀请码**:如需更换,改函数环境变量 `INVITE_CODE` 并重新发布
