# ROI 计算器 - EdgeOne 上线操作指南(面向首次操作)

> 配套文件:项目目录中的 `worker.js`(后端)与 `部署说明.md`(速览)。
> 本指南按"第一次上手"的节奏写,跟着做即可。控制台菜单名若与截图有差异,以控制台实际显示为准。

---

## 第 0 步:准备

| 需要 | 说明 |
|---|---|
| 腾讯云账号 | 已实名认证(个人/企业均可) |
| 域名(可选) | 有已备案域名最好;没有可先用 EdgeOne 默认测试域名跑通全流程,之后再绑正式域名 |
| 本地文件 | `Web_Project_ROI` 目录(确认含 index.html、login.html、css/、images/、worker.js) |

## 第 1 步:开通 EdgeOne 并接入站点

1. 登录[腾讯云控制台](https://console.cloud.tencent.com),顶部搜索 **EdgeOne**,进入产品页
2. 点击「开通服务」(按引导完成,通常免费开通)
3. 进入 EdgeOne 控制台 → 「站点」→「创建站点」:
   - **有域名**:填你的域名(如 `roi.公司域名.com`),选择接入方式(一般选 CNAME 接入),按提示到域名 DNS 服务商添加记录,等待状态变"生效"
   - **没有域名**:选择使用 EdgeOne 提供的默认测试域名,跳过 DNS 步骤

> 站点生效后,先别急着部署,按下面顺序建 KV → 传页面 → 建函数 → 配路由。

## 第 2 步:创建 KV 存储

1. 左侧菜单「数据存储 → KV 存储」
2. 「新建命名空间」:名称填 `roi-kv`,确认创建
3. 记下这个命名空间的 **ID**(第 5 步绑定要用)

## 第 3 步:部署静态页面(Pages)

1. 左侧菜单「边缘函数 → 边缘函数」旁边的 **Pages**(或「部署 → Pages」)
2. 「新建项目」→ 选「上传静态文件」
3. 上传时**只传这 4 个**,不要传 worker.js / 部署说明.md:
   ```
   index.html
   login.html
   css/style.css
   images/logo.png
   ```
4. 部署完成,得到一个访问地址(如 `https://xxxx.edgeone.app` 或你的域名)
5. 先访问 `login.html` 确认页面能打开(此时登录会失败属正常,后端还没建)

## 第 4 步:创建边缘函数

1. 左侧「边缘函数 → 边缘函数」→「新建函数」
2. 名称:`roi-api`
3. 打开本机 `worker.js`,**全选复制**,粘贴进函数代码编辑器,保存
4. 配置「变量与绑定」:
   - **KV 绑定**:选择第 2 步的 `roi-kv`,**变量名必须填 `KV`**(代码里读 `env.KV`)
   - **环境变量**:`INVITE_CODE = ROI2026`(可改成你们内部码)
5. 点击「发布/部署」,让函数生效

## 第 5 步:配置路由(让 /api/* 走函数)

1. 进入你的「站点」→「规则引擎」(部分版本在函数详情页有「触发规则」)
2. 新建一条规则:
   - 匹配条件:**路径前缀 = /api/**
   - 执行动作:**边缘函数 → roi-api**
3. 保存并发布规则
4. 效果:`/api/xxx` 由边缘函数处理;其余路径(html/css/images)走 Pages 静态

## 第 6 步:验证后端是否可用(不依赖前端)

在浏览器打开你的站点,按 F12 → Console,执行:

```js
fetch('/api/auth/login', {method:'POST',headers:{'Content-Type':'application/json'},
  body:JSON.stringify({account:'admin',pwd:'admin123'})}).then(r=>r.json()).then(console.log)
```

- 期望返回:`{ok:true, token:"...", user:{name:"管理员", level:1}}`
- 若返回 `{ok:false,msg:"..."}`:看提示(如"账号待审核/密码错误"→ 说明函数已通,首次调用已自动建好 admin,密码就是 admin123)
- 若网络 404 / 500 / 超时:见下方「常见问题」

再用管理员 token 测一个接口(在 Console 连续执行):

```js
// 第一步登录拿到的 token 存到变量
var T = '<上一步的token>';
fetch('/api/projects', {headers:{'Authorization':'Bearer '+T}}).then(r=>r.json()).then(console.log)
```

期望:`{ok:true, projects:{}}`

## 第 7 步:数据迁移(把本地数据搬上云,可选)

本地演示版数据在浏览器 localStorage。迁移方式:

1. 在有数据的电脑浏览器,打开本地版 `index.html`,F12 Console 执行:
   ```js
   console.log(localStorage.getItem('roi_calculator_projects'))
   ```
2. 复制输出的大 JSON
3. 在线上站点 Console 执行(用管理员 token):
   ```js
   var T='<token>';
   var projects = JSON.parse('<第1步复制的内容,注意转义>');
   // 逐项目写入;若数据量大,告诉我,我给你一段批量导入脚本
   ```

## 第 8 步:前端切换到云端 API(重要)

当前 `login.html` / `index.html` 还是**本地演示模式**(数据存浏览器 localStorage),**不会自动连 /api**。要在线上真正用云端账号和共享数据,需要做前端 API 切换改造——这一步我可以远程给你改好代码,你重新上传 Pages 即可。**上线前告诉我,我帮你完成这一步再正式使用。**

## 常见问题排查

| 现象 | 原因/处理 |
|---|---|
| 访问 login.html 404 | Pages 没传上去,或访问路径不对(确认完整地址,可能带目录前缀) |
| `/api` 请求 404 | 路由规则未生效:确认规则匹配 `/api/*` 且已发布;函数已发布 |
| `/api` 返回 500 | 函数代码问题或 KV 未绑定:检查绑定变量名是否为 `KV`、命名空间选对 |
| 登录提示"账号不存在" | 首次调用应已建 admin;若 KV 里 users 异常,删除 `users` 键后重新触发即可重建 |
| 忘记 admin 密码 | KV 控制台删除 `users` 键 → 任意调一次接口自动重建 `admin/admin123` |
| 前端登录报错但 API 通 | 前端还是本地模式,属正常,等第 8 步切换 |
| 跨域报错 | 同域部署不会出现;若用独立域名部署前后端,worker 已带 CORS 头,仍异常再反馈 |

## 上线检查清单

- [ ] login.html 能打开
- [ ] `/api/auth/login` 用 admin/admin123 返回 ok
- [ ] 注册(邀请码)→ 管理员审核 → 能登录
- [ ] 新建项目填负责人,列表标题右侧显示负责人标签
- [ ] 1级全改、2级仅本人负责项目可改(其余只读)
- [ ] 前端已切换 API 模式(第 8 步完成)
- [ ] HTTPS 已生效(EdgeOne 默认)

---

操作过程中遇到任何一步卡住,把**界面截图或报错文字**发给我,我帮你定位下一步。
